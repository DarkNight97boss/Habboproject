<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <withJansi>false</withJansi>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%-14thread] %highlight(%-5level) %cyan(%-36logger{36}) - %replace(%msg){'[\r\n]+', ' '}%n</pattern>
        </encoder>
    </appender>

    <appender name="FileDebug" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logging/debug.txt</file>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>DEBUG</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logging/debug.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %level %logger - %replace(%msg){'[\r\n]+', ' '}%n</pattern>
        </encoder>
    </appender>

    <appender name="FileErrors" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logging/errors/runtime.txt</file>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logging/errors/runtime.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %level %logger - %replace(%msg){'[\r\n]+', ' '}%n</pattern>
        </encoder>
    </appender>

    <appender name="FileErrorsSql" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logging/errors/sql.txt</file>
        <filter class="com.eu.habbo.util.logback.SqlExceptionFilter" />
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logging/errors/sql.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %level %logger - %replace(%msg){'[\r\n]+', ' '}%n</pattern>
        </encoder>
    </appender>

    <!--
        Async wrappers around the file appenders. Under load (51 log statements
        in Room.java fire on every room tick × N rooms × M users) the I/O cost
        of synchronous disk writes stalls the game-thread tick budget. Async
        moves the format+write to a dedicated thread, returns immediately, and
        drops only when the queue is *completely* full. Console stays sync so
        we don't lose the last few lines on a crash. Discarding policy: drop
        TRACE/DEBUG/INFO when the queue is 80% full; keep WARN/ERROR always.
    -->
    <appender name="AsyncFileDebug" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>4096</queueSize>
        <discardingThreshold>820</discardingThreshold>
        <neverBlock>true</neverBlock>
        <includeCallerData>false</includeCallerData>
        <appender-ref ref="FileDebug" />
    </appender>

    <appender name="AsyncFileErrors" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>2048</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <neverBlock>false</neverBlock>
        <includeCallerData>true</includeCallerData>
        <appender-ref ref="FileErrors" />
    </appender>

    <appender name="AsyncFileErrorsSql" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>2048</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <neverBlock>false</neverBlock>
        <includeCallerData>true</includeCallerData>
        <appender-ref ref="FileErrorsSql" />
    </appender>

    <logger name="io.netty">
        <level value="info"/>
    </logger>

    <logger name="com.zaxxer.hikari">
        <level value="error"/>
    </logger>

    <!-- Tighten noisy hot-path loggers so they don't dominate the async queue. -->
    <logger name="com.eu.habbo.habbohotel.rooms.Room">
        <level value="warn"/>
    </logger>
    <logger name="com.eu.habbo.habbohotel.rooms.RoomManager">
        <level value="warn"/>
    </logger>
    <logger name="com.eu.habbo.habbohotel.items.interactions">
        <level value="warn"/>
    </logger>

    <root level="info">
        <appender-ref ref="Console" />
        <appender-ref ref="AsyncFileDebug" />
        <appender-ref ref="AsyncFileErrors" />
        <appender-ref ref="AsyncFileErrorsSql" />
    </root>
</configuration>
