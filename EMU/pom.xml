<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!--
        groupId + artifactId LASCIATI INVARIATI per non rompere:
          - il manifest mainClass del jar-with-dependencies
          - il nome file Habbo-*-jar-with-dependencies.jar referenziato in
            start_clean_stack.ps1 e EMU/Dockerfile
          - eventuali plugin Maven esterni che dipendono da com.eu.habbo:Habbo
        Il rebrand "Asteria Core" e' applicato a <name> e <description> +
        documentazione + banner di runtime.
    -->
    <groupId>com.eu.habbo</groupId>
    <artifactId>Habbo</artifactId>
    <version>3.5.5</version>
    <name>Asteria Core</name>
    <description>Asteria Core - server di gioco real-time per ambienti virtuali. Fork ribrandizzato di Arcturus Morningstar (GPL-3.0). Mantiene compatibilita' col client e con lo schema database upstream.</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <additionalparam>-Xdoclint:none</additionalparam>
    </properties>

    <profiles>
        <profile>
            <id>java8-doclint-disabled</id>
            <activation>
                <jdk>[1.8,)</jdk>
            </activation>
            <properties>
                <javadoc.opts>-Xdoclint:none</javadoc.opts>
            </properties>
        </profile>
    </profiles>

    <build>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.15.0</version>
                <configuration>
                    <source>1.8</source>
                    <target>1.8</target>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>3.8.0</version>
                <configuration>
                    <descriptorRefs>
                        <descriptorRef>jar-with-dependencies</descriptorRef>
                    </descriptorRefs>
                    <archive>
                        <manifest>
                            <mainClass>com.eu.habbo.Emulator</mainClass>
                        </manifest>
                    </archive>
                </configuration>
                <executions>
                    <execution>
                        <id>make-assembly</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>3.12.0</version>
                <configuration>
                    <additionalOptions><additionalOption>-Xdoclint:none</additionalOption></additionalOptions>
                    <show>public</show>
                </configuration>
            </plugin>

            <!--
                Surefire 3.x e' obbligatorio per JUnit5 (le 2.x non scoprono
                @Test JUnit Jupiter). 3.2.5 e' l'ultima compatibile Java 8.
                Failsafe per test "integration" (suffisso IT) e' opzionale e
                lo aggiungeremo quando avremo testcontainers.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.6</version>
                <configuration>
                    <!-- Niente fork (test puri unit, nessuna risorsa esterna). -->
                    <forkCount>1</forkCount>
                    <reuseForks>true</reuseForks>
                    <!-- Continua a buildare anche se i test falliscono?
                         NO: se un test sec/concorrenza fallisce e' un bug
                         critico, deve bloccare il package. -->
                    <testFailureIgnore>false</testFailureIgnore>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <repositories>
        <repository>
            <id>central</id>
            <url>https://repo1.maven.org/maven2/</url>
        </repository>
        <repository>
            <id>mvnrepo</id>
            <url>https://mvnrepository.com/</url>
        </repository>
    </repositories>

    <dependencies>
        <!-- Netty -->
        <dependency>
            <groupId>io.netty</groupId>
            <artifactId>netty-all</artifactId>
            <version>4.1.115.Final</version>
        </dependency>
        <!--
            Native epoll transport. netty-all ships the Java side but the
            platform .so is in a separate artifact with a classifier. We pull
            the linux-x86_64 binary so on production Linux Server picks up
            EpollEventLoopGroup automatically (Epoll.isAvailable() returns
            true only when both the JNI class AND the native library load).
            On Windows/macOS the artifact is inert and Server falls back to
            NIO. Adds ~600KB to the shaded JAR.
        -->
        <dependency>
            <groupId>io.netty</groupId>
            <artifactId>netty-transport-native-epoll</artifactId>
            <version>4.1.115.Final</version>
            <classifier>linux-x86_64</classifier>
        </dependency>

        <!-- GSON (>=2.8.9 chiude CVE-2022-25647 DoS via writeReplace deser.) -->
        <dependency>
            <groupId>com.google.code.gson</groupId>
            <artifactId>gson</artifactId>
            <version>2.14.0</version>
        </dependency>

        <!-- MySQL Connector (>=8.0.33 chiude CVE-2022-21363 + CVE-2023-21971 JDBC privesc) -->
        <dependency>
            <groupId>mysql</groupId>
            <artifactId>mysql-connector-java</artifactId>
            <version>8.0.33</version>
            <scope>runtime</scope>
        </dependency>

        <!-- Trove -->
        <dependency>
            <groupId>net.sf.trove4j</groupId>
            <artifactId>trove4j</artifactId>
            <version>3.0.3</version>
            <scope>compile</scope>
        </dependency>

        <!-- https://mvnrepository.com/artifact/com.zaxxer/HikariCP -->
        <dependency>
            <groupId>com.zaxxer</groupId>
            <artifactId>HikariCP</artifactId>
            <version>3.4.3</version>
            <scope>compile</scope>
        </dependency>

        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
            <version>3.20.0</version>
            <scope>compile</scope>
        </dependency>

        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-math3</artifactId>
            <version>3.6.1</version>
            <scope>compile</scope>
        </dependency>

        <!-- jsoup (>=1.15.3 chiude CVE-2022-36033 XSS bypass + CVE-2021-37714 DoS) -->
        <dependency>
            <groupId>org.jsoup</groupId>
            <artifactId>jsoup</artifactId>
            <version>1.22.2</version>
            <scope>compile</scope>
        </dependency>

        <!-- logback-classic (>=1.2.13 chiude CVE-2023-6378 deserialization DoS + CVE-2021-42550 JNDI). -->
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>1.2.13</version>
            <scope>compile</scope>
        </dependency>

        <dependency>
            <groupId>org.fusesource.jansi</groupId>
            <artifactId>jansi</artifactId>
            <version>1.18</version>
        </dependency>

        <dependency>
            <groupId>joda-time</groupId>
            <artifactId>joda-time</artifactId>
            <version>2.14.2</version>
        </dependency>

        <!--
            Caffeine: in-process cache with strong eviction policies. 2.x is the
            Java 8 compatible branch (3.x requires Java 11). Used for hot read
            paths where the DB / scan cost is non-trivial but the data changes
            rarely (e.g. catalog page lookup by name). Centralised in
            com.eu.habbo.core.HotCache so call sites stay decoupled from the lib.
        -->
        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
            <version>2.9.3</version>
        </dependency>

        <!--
            Jedis: client Redis (single-instance + Sentinel). 4.4.x e' l'ultima
            compatibile Java 8 (5.x richiede Java 11). Usato come backing store
            opzionale per rate-limiter e session-cache CROSS-instance quando
            l'emulatore gira su >1 nodo (Tier 2 architettura nel deliverable).
            Se redis.host e' vuoto, RedisClient resta in modalita' "disabled"
            e nessuno si connette -> overhead zero su single-node.
        -->
        <dependency>
            <groupId>redis.clients</groupId>
            <artifactId>jedis</artifactId>
            <version>4.4.6</version>
        </dependency>

        <!--
            JUnit 5 (Jupiter). 5.10.x e' l'ultima minor compatibile col target
            Java 8; 5.11+ richiede Java 17. Scope=test => non finisce nel JAR
            production. Aggregator "junit-jupiter" tira API + engine + params.
        -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.10.2</version>
            <scope>test</scope>
        </dependency>

        <!--
            Testcontainers: container ephemeral per integration test DB-backed.
            1.19.x e' l'ultima compatibile Java 8 (la 1.20+ richiede 11).
            Estensione JUnit5 + immagine MariaDB. Lo skipa automaticamente se
            il daemon Docker non e' raggiungibile dall'host CI (vedi @EnabledIf
            nel test) cosi' la pipeline non rompe quando docker non c'e'.
        -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers</artifactId>
            <version>1.19.7</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>1.21.4</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>mariadb</artifactId>
            <version>1.21.4</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>